#include <stdio.h>
#include <math.h>
int main()
{
    int temp, n=0;
    scanf("%d", &temp);
    while(temp)
    {
        temp = temp / 10; // dem coi so co bao nhieu chu so
        n++;
        printf("A");

    }
    }
